import { RandomGenerator } from 'pure-rand';
export declare function exponential(gen: RandomGenerator, rate: number): Generator<number>;
export declare function uniform(gen: RandomGenerator, min: number, max: number): Generator<number>;
